# SUBMISSION GENERATION:

`--candidates-parquet` are the candidates generated by the reranker model.
## STEP 1:
```bash
uv run --no-sync python -u scripts/launchers/gambling_updated.py --model 8 \
  --instruction-prompts none \
  --blind-path ../../data/talkpl-ai/TalkPlayData-Challenge-Blind-B/data/test-00000-of-00001.parquet \
  --query-cache-dir ../../models/retrieval_text_towers/Qwen__Qwen3-Embedding-8B/dense_blindb_all_query_len512_poollast \
  --candidates-parquet ../../models/reranker_oof/blind_b_retrain/no_filter_v5/v2_blind_last/candidates/cand_no_filter_v5_v2_blind_last_blind_b.parquet \
  --candidates-topn 100 --candidates-max 30 --candidates-kind blind_b \
  --qwen-pool-k 10 --anchor-pool-k 0 --dense-anchor-recency --w-dense 1.5 --w-audio 0.85 --w-dense-anchor 5.0 \
  --dense-anchor-center 0.4 --fallback-depth 120 \
  --output-path FINAL29.json --fallback-content-rerank --no-decade \
  --release-date-weight 2.0 --w-rel-registrant 0.5 --w-rel-raretag 0.5 --w-rel-artist 0.5 --rel-tag-df 5

```

**No test tracks filtering**: omit `--restrict-to-catalog` and `--restrict-id-column` in the above command.


## STEP 2:
`--fusion-turn1` is the json of submission generated by the reranker model.
`FINAL29.json` is the json of submission generated by previous script.
```bash
uv run --no-sync python 96_graft_turn1.py \
  --fused-turn1 ../../models/reranker_oof/blind_b_retrain/no_filter_v5/v2_blind_last/submissions/blind_b_no_filter_v5_v2_blind_last.json \
  FINAL29.json
```
## STEP 3:
`FINAL29_t1graft.json` is the json of submission generated by previous script.
`--b` is the json of submission generated by the reranker model.
``` bash
python 98_top1_graft.py \
  --a FINAL29_t1graft.json \
  --b ../../models/reranker_oof/blind_b_retrain/no_filter_v5/v2_blind_last/submissions/blind_b_no_filter_v5_v2_blind_last.json \
  --out LAST.json
```
